<view class="page-upload">
  <view class="title">菜品图片上传工具</view>

  <view class="section">
    <view class="section-title">方式一：从云存储自动关联</view>
    <view class="desc">先在云开发控制台 → 云存储 → 上传图片到 dishes/ 目录，然后点击下方按钮自动关联到数据库</view>
    <button class="btn" bindtap="onAutoLink">自动关联云存储图片</button>
  </view>

  <view class="section">
    <view class="section-title">方式二：从本地选择上传</view>
    <view class="desc">选择手机相册中的图片，逐批上传到云存储</view>
    <button class="btn" bindtap="onChooseAndUpload" disabled="{{uploading}}">选择图片上传</button>
  </view>

  <view class="section" wx:if="{{fileCount > 0}}">
    <view class="section-title">方式三：手动更新数据库</view>
    <view class="desc">使用已上传的 {{fileCount}} 张图片更新数据库</view>
    <button class="btn" bindtap="onUpdateDB">更新数据库</button>
  </view>

  <view class="status" wx:if="{{status}}">
    <text>{{status}}</text>
  </view>

  <view class="progress" wx:if="{{uploading}}">
    <progress percent="{{progress / total * 100}}" activeColor="#FF6B35" stroke-width="6"/>
    <text>{{progress}} / {{total}}</text>
  </view>

  <view class="tips">
    <view class="tips-title">图片命名规则</view>
    <view class="tip">文件名必须与数据库中的 filename 对应，如：</view>
    <view class="tip">fanqie_chaodan.jpg → 番茄炒蛋</view>
    <view class="tip">hongshao_rou.jpg → 红烧肉</view>
    <view class="tip">完整文件名列表见 crawler/download_images.py</view>
  </view>
</view>
